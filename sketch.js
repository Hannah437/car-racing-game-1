var hypnoticBall, database;
var position;
var playerCount =0, form,player,gameState =0, game;

function setup(){
  database = firebase.database();
  
  createCanvas(500,500);
  form = new Form();
  form.display();
 
}

function draw(){
  
  
}

